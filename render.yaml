services:
  - type: web
    name: cravebites-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server_mongo.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
      - key: MONGODB_URI
        sync: false
